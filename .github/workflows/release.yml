name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      build_dist: true
      upload_artifacts: true
      version_suffix: ${{ github.ref_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: dltchatplugin-*

      - name: Flatten artifacts
        run: |
          rm -rf release_assets
          mkdir release_assets
          find artifacts -name "*.zip" -exec cp {} release_assets/ \;
          find artifacts -name "*.tar.gz" -exec cp {} release_assets/ \;
          find artifacts -name "*.dll" -exec cp {} release_assets/ \;
          find artifacts -name "*.so" -exec cp {} release_assets/ \;
          ls -R release_assets

      - name: Get Changelog Entry
        id: changelog
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          # Extract section between current version and next ## header
          sed -n "/^## \[$VERSION\]/,/^## /p" CHANGELOG.md | sed '$d' > release_body.md
          if [ ! -s release_body.md ]; then
            echo "Release $VERSION" > release_body.md
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body_path: release_body.md
          files: release_assets/*
          draft: false
          prerelease: false
