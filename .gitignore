# Build directories
build_dev/
build/
build_tests/
build-*/
build_plugin/
build_audit/
build_msvc*/
build_qt*/
cmake-build-*/
out/

# Local DLT Viewer SDK install produced from the ARTIST8 fork build
_dltsdk*/

# Compiled files
*.o
*.obj
*.exe
*.dll
*.so
*.dylib
*.a
*.lib

# Qt build files
moc_*.cpp
qrc_*.cpp
ui_*.h
*.moc
*.qmlc
*.qmljsc

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
Makefile

# Test outputs
Testing/
*.log
test_results/

# Temporary files
*.tmp
*.bak
*.orig
*.rej

# DLT log and project files
*.dlt
*.dlp
# ...but the bundled default filter set IS part of the plugin and must be tracked
!src/resources/default_filters.dlp

# Local ARTIST8 DLT-Viewer fork (qdlt SDK source) — never commit (see CLAUDE.md)
ART-DLT-viewer-ARTIST8-*/
# Vendored reference manuals (large binaries)
dlt_viewer_user_manual.pdf
dlt_viewer_plugins_programming_guide.pdf
# Local-only build inputs (Qt install, staged qdlt SDK, root build dirs, helper script)
/Qt/
/_dltsdk5/
/build_qt5/
_build_msvc.bat

# Package files
*.tar.gz
*.zip

# OS specific
Thumbs.db

# Local DLT Viewer SDK source (custom ARTIST8 fork) — only used locally to
# build the qdlt SDK; must never be committed to this repo.
ART-DLT-viewer-ARTIST8-*/

# User config
dlt_chat_plugin.ini

# AI/Claude configuration
.claude/

# Backup patches
backup_patches/

# Text/work files
prompts.txt
r.txt

# Vendor downloaded sources
vendor/_deps/
vendor/_build/
vendor/downloads/

# Generated version header
generated/

# IDE
.qt/
.vs/
*.user
*.suo

# Qt installation (local SDK, 300MB+ — never stash/include)
6.7.3/
5.15.2/
/Qt/

# Installer logs
aqtinstall.log

# Git hooks (local developer setup, not repo policy)
.githooks/

# Generated documentation (slide deck from external tool)
docs/

# Release artifacts
dltchatplugin-*.zip
dltchatplugin-*.tar.gz
releases.json
build_release/
build_tests_check/
build_msvc_*/
